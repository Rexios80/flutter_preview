name: flutter_preview_example
description: "A new Flutter project."
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.3.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  flutter_preview:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  rexios_lints: ^6.0.1

flutter:
  uses-material-design: true
